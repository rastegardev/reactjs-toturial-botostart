import React, { useEffect } from "react";

import useTitle from "../hooks/useTitle";

const Landing = () => {
  useTitle("Lainding");

  return (
    <>
      <h1>Landing Page</h1>
    </>
  );
};

export default Landing;

import React, { useEffect } from "react";

import useTitle from "../hooks/useTitle";

const LoginPage = () => {
  useTitle("Login");
  return (
    <div>
      <h1>Login page</h1>
    </div>
  );
};

export default LoginPage;
